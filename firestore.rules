rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ⚠️ ALLOW ALL: Good for testing.
      // Use this now to make sure your app works immediately.
      allow read, write: if true;
    }
  }
}
